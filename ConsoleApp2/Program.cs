using System;

namespace ConsoleApp2;

internal class Program {

    /**
     * Another way of doing the main entrance of the program.
     * 
     * Ctrl + c + k = make it into comments
     * Ctrl + u = undo comments
     * Ctrl + k + g = The } gets into the right place. Make things pretty.
     */

    public static void Main(string[] args)
    {
        Console.WriteLine("Hello, World!");
    }
}
